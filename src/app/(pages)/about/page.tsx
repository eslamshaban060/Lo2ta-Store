import React from "react";
import Header from "@/components/header/header";

const page = () => {
  return (
    <div>
      {/* header component  */}
      <Header />
    </div>
  );
};

export default page;
